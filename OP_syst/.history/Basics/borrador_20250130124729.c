#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <time.h>

void swap(int *a, int *b);

int main() {
    int a =10, b=20;
    printf("Before swapping: a = %d, b= %d\n", a, b);
    swap(&a, &b);
    printf("Before swapping: a = %d, b= %d\n", a, b);
   
    return 0;
    
}

void swap(int *a, int *b){
    int c;
    c=a

}


