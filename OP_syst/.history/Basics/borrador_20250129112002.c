#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <time.h>

int main() {
    int numbers[5], sum;
    printf("Enter 5 numbers: ");
    for (int i =1; i<=sizeof(numbers); ++i){
        printf("Number %d: ", i);
        scanf("%d", numbers[i]);
        

    }

}