#include <stdio.h>
#include <pthread.h>
#include <stdlib.h>

/*
pthread_cond_t cond; 
pthread_cond_init(&cond, NULL);	Initialize a condition variable
pthread_cond_wait(&cond, &mutex);	Wait for a condition to be signaled
pthread_cond_signal(&cond);	Wake up one waiting thread
pthread_cond_broadcast(&cond);	Wake up all waiting threads
pthread_cond_destroy(&cond);	Destroy the condition variable

*/

int main(){


    return 0;
}


