#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <time.h>

int main() {
    int numbers[5], top=0;
    printf("Enter 5 numbers:\n");
    for (int i =0; i<sizeof(numbers)/sizeof(int); ++i){
        printf("Number %d: ", i+1);
        scanf("%d", &numbers[i]);
        if
        
    }
    printf("The largest is: %d \n", top);
    return 0;
}